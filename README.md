# API Users
User Directory with Search and Company Filter  A simple web project that fetches a list of users from an API and displays them in a list. Users can be filtered by name through a search input and by company using a dropdown. Includes a reset button to clear filters and shows a message when no users match the search criteria.
